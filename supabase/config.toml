project_id = "iekwvvihjwghosqdxrdi"

# verify_jwt kept false for CORS/Lovable invocation compatibility.
# Admin auth is enforced in-function via assertAdminRequest(req) — that check is MANDATORY.
[functions.create-product-ai]
verify_jwt = false

[functions.get-customer-orders]
verify_jwt = false

[functions.shopify-admin-proxy]
verify_jwt = false
